diff --git a/agent/agent_init.py b/agent/agent_init.py
index 675130a88..ac4575db4 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -1078,6 +1078,7 @@ def init_agent(
                 agent._memory_store = MemoryStore(
                     memory_char_limit=mem_config.get("memory_char_limit", 2200),
                     user_char_limit=mem_config.get("user_char_limit", 1375),
+                    contact_user_id=user_id,
                 )
                 agent._memory_store.load_from_disk()
         except Exception:
diff --git a/agent/agent_runtime_helpers.py b/agent/agent_runtime_helpers.py
index 09eccef5f..c72f73936 100644
--- a/agent/agent_runtime_helpers.py
+++ b/agent/agent_runtime_helpers.py
@@ -1709,6 +1709,7 @@ def invoke_tool(agent, function_name: str, function_args: dict, effective_task_i
                 sort=function_args.get("sort"),
                 db=session_db,
                 current_session_id=agent.session_id,
+                contact_user_id=getattr(agent, "_user_id", None),
             )
         )
     elif function_name == "memory":
diff --git a/agent/tool_executor.py b/agent/tool_executor.py
index fc3667edb..16652e649 100644
--- a/agent/tool_executor.py
+++ b/agent/tool_executor.py
@@ -897,6 +897,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
                     sort=function_args.get("sort"),
                     db=session_db,
                     current_session_id=agent.session_id,
+                    contact_user_id=getattr(agent, "_user_id", None),
                 )
             tool_duration = time.time() - tool_start_time
             if agent._should_emit_quiet_tool_messages():
diff --git a/cron/scheduler.py b/cron/scheduler.py
index d0240e9af..675d7263d 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -716,11 +716,10 @@ def _deliver_result(job: dict, content: str, adapters=None, loop=None) -> Option
         task_name = job.get("name", job["id"])
         job_id = job.get("id", "")
         delivery_content = (
-            f"Cronjob Response: {task_name}\n"
-            f"(job_id: {job_id})\n"
-            f"-------------\n\n"
-            f"{content}\n\n"
-            f"To stop or manage this job, send me a new message (e.g. \"stop reminder {task_name}\")."
+            f"⏰ *Lembrete agendado* 🔔\n"
+            f"_{task_name}_\n"
+            f"─────────────────────\n\n"
+            f"{content}"
         )
     else:
         delivery_content = content
diff --git a/gateway/platforms/whatsapp.py b/gateway/platforms/whatsapp.py
index 7ece37dbc..01aec6e92 100644
--- a/gateway/platforms/whatsapp.py
+++ b/gateway/platforms/whatsapp.py
@@ -250,7 +250,11 @@ class WhatsAppAdapter(BasePlatformAdapter):
     def __init__(self, config: PlatformConfig):
         super().__init__(config, Platform.WHATSAPP)
         self._bridge_process: Optional[subprocess.Popen] = None
-        self._bridge_port: int = config.extra.get("bridge_port", 3000)
+        _env_port = os.environ.get("BRIDGE_PORT", "")
+        self._bridge_port: int = (
+            int(_env_port) if _env_port.isdigit()
+            else config.extra.get("bridge_port", 3000)
+        )
         self._bridge_script: Optional[str] = config.extra.get(
             "bridge_script",
             str(self._DEFAULT_BRIDGE_DIR / "bridge.js"),
diff --git a/gateway/slash_access.py b/gateway/slash_access.py
index e4a398dc1..78dccac36 100644
--- a/gateway/slash_access.py
+++ b/gateway/slash_access.py
@@ -39,16 +39,13 @@ from typing import Any, FrozenSet, Iterable, Optional, Tuple
 
 
 # Slash commands that MUST stay reachable for any allowed user, even when
-# slash gating is enabled and the user has no commands listed. Without this
-# carve-out, a non-admin user has no way to discover what they can or
-# can't do (``/help``, ``/whoami``) and no way to see what state the agent
-# is in (``/status``). These mirror the smallest set of read-only commands
-# we'd hand to a guest. Operators can still narrow this further by writing
-# their own ``user_allowed_commands`` (this set is only the implicit
-# fallback floor — anything in ``user_allowed_commands`` overrides it
-# additively, never restrictively).
+# slash gating is enabled and the user has no commands listed.
+#
+# Root policy for this multi-tenant WhatsApp deployment: regular users get
+# only `/whoami` for self-diagnosis. `/help` exposes the command surface and
+# is intentionally admin-only unless the operator explicitly lists it in
+# `user_allowed_commands`.
 _ALWAYS_ALLOWED_FOR_USERS: FrozenSet[str] = frozenset({
-    "help",
     "whoami",
 })
 
diff --git a/hermes_cli/tips.py b/hermes_cli/tips.py
index 3a17055e7..197abe172 100644
--- a/hermes_cli/tips.py
+++ b/hermes_cli/tips.py
@@ -142,7 +142,7 @@ TIPS = [
     "text_to_speech converts text to audio — plays as voice bubbles on Telegram.",
     "send_message can reach any connected messaging platform from within a session.",
     "The todo tool helps the agent track complex multi-step tasks during a session.",
-    "session_search performs full-text search across ALL past conversations.",
+    "session_search performs full-text search across your past conversations (scoped to the current contact in gateway sessions).",
     "The agent automatically saves preferences, corrections, and environment facts to memory.",
     "mixture_of_agents routes hard problems through 4 frontier LLMs collaboratively.",
     "Terminal commands support background mode with notify_on_complete for long-running tasks.",
diff --git a/hermes_state.py b/hermes_state.py
index ca7ea5bd0..d284fadd9 100644
--- a/hermes_state.py
+++ b/hermes_state.py
@@ -1594,6 +1594,7 @@ class SessionDB:
         include_archived: bool = False,
         archived_only: bool = False,
         id_query: str = None,
+        user_id: str = None,
     ) -> List[Dict[str, Any]]:
         """List sessions with preview (first user message) and last active timestamp.
 
@@ -1659,6 +1660,9 @@ class SessionDB:
         if min_message_count > 0:
             where_clauses.append("s.message_count >= ?")
             params.append(min_message_count)
+        if user_id is not None:
+            where_clauses.append("s.user_id = ?")
+            params.append(user_id)
         if archived_only:
             where_clauses.append("s.archived = 1")
         elif not include_archived:
@@ -2778,6 +2782,7 @@ class SessionDB:
         offset: int = 0,
         sort: str = None,
         include_inactive: bool = False,
+        user_id: str = None,
     ) -> List[Dict[str, Any]]:
         """
         Full-text search across session messages using FTS5.
@@ -2802,6 +2807,11 @@ class SessionDB:
 
         Rewound (``active=0``) rows are excluded by default. Pass
         ``include_inactive=True`` to search every row.
+
+        ``user_id`` scopes the search to a single contact (``s.user_id = ?``).
+        Used by gateway sessions for per-contact isolation so an agent serving
+        one contact cannot recall another contact's transcripts. ``None``
+        (CLI / non-gateway) searches across all contacts as before.
         """
         if not self._fts_enabled:
             return []
@@ -2853,6 +2863,10 @@ class SessionDB:
             where_clauses.append(f"m.role IN ({role_placeholders})")
             params.extend(role_filter)
 
+        if user_id is not None:
+            where_clauses.append("s.user_id = ?")
+            params.append(user_id)
+
         where_sql = " AND ".join(where_clauses)
         params.extend([limit, offset])
 
@@ -2927,6 +2941,9 @@ class SessionDB:
                 if role_filter:
                     tri_where.append(f"m.role IN ({','.join('?' for _ in role_filter)})")
                     tri_params.extend(role_filter)
+                if user_id is not None:
+                    tri_where.append("s.user_id = ?")
+                    tri_params.append(user_id)
                 tri_sql = f"""
                     SELECT
                         m.id,
@@ -2982,6 +2999,9 @@ class SessionDB:
                 if role_filter:
                     like_where.append(f"m.role IN ({','.join('?' for _ in role_filter)})")
                     like_params.extend(role_filter)
+                if user_id is not None:
+                    like_where.append("s.user_id = ?")
+                    like_params.append(user_id)
                 like_sql = f"""
                     SELECT m.id, m.session_id, m.role,
                            substr(m.content,
diff --git a/scripts/whatsapp-bridge/bridge.js b/scripts/whatsapp-bridge/bridge.js
index 5723d8b54..69fcb9248 100644
--- a/scripts/whatsapp-bridge/bridge.js
+++ b/scripts/whatsapp-bridge/bridge.js
@@ -207,6 +207,19 @@ async function startSocket() {
       console.log('\n📱 Scan this QR code with WhatsApp on your phone:\n');
       qrcode.generate(qr, { small: true });
       console.log('\nWaiting for scan...\n');
+      try { writeFileSync('/tmp/qr.txt', qr); } catch (e) {}
+      // Write QR payload to profile qr dir so the dashboard can serve it
+      try {
+        // session is at {profile}/platforms/whatsapp/session → go up 3 to reach profile root
+        // or at {profile}/session → go up 1. Detect by checking grandparent name.
+        const sessionParent = path.basename(path.resolve(SESSION_DIR, '..'));
+        const profileRoot = sessionParent === 'whatsapp'
+          ? path.resolve(SESSION_DIR, '..', '..', '..')
+          : path.resolve(SESSION_DIR, '..');
+        const qrDir = path.join(profileRoot, 'qr');
+        mkdirSync(qrDir, { recursive: true });
+        writeFileSync(path.join(qrDir, 'qr-connect.txt'), qr);
+      } catch (e) {}
     }
 
     if (connection === 'close') {
@@ -273,15 +286,21 @@ async function startSocket() {
           continue;
         }
 
-        // Self-chat mode: only allow messages in the user's own self-chat
-        // WhatsApp now uses LID (Linked Identity Device) format: 67427329167522@lid
-        // AND classic format: 34652029134@s.whatsapp.net
-        // sock.user has both: { id: "number:10@s.whatsapp.net", lid: "lid_number:10@lid" }
-        const myNumber = (sock.user?.id || '').replace(/:.*@/, '@').replace(/@.*/, '');
-        const myLid = (sock.user?.lid || '').replace(/:.*@/, '@').replace(/@.*/, '');
-        const chatNumber = chatId.replace(/@.*/, '');
-        const isSelfChat = (myNumber && chatNumber === myNumber) || (myLid && chatNumber === myLid);
-        if (!isSelfChat) continue;
+        if (WHATSAPP_MODE === 'self-chat') {
+          // Self-chat mode: only allow messages in the user's own self-chat.
+          // WhatsApp now uses LID (Linked Identity Device) format: 67427329167522@lid
+          // AND classic format: 34652029134@s.whatsapp.net
+          // sock.user has both: { id: "number:10@s.whatsapp.net", lid: "lid_number:10@lid" }
+          const myNumber = (sock.user?.id || '').replace(/:.*@/, '@').replace(/@.*/, '');
+          const myLid = (sock.user?.lid || '').replace(/:.*@/, '@').replace(/@.*/, '');
+          const chatNumber = chatId.replace(/@.*/, '');
+          const isSelfChat = (myNumber && chatNumber === myNumber) || (myLid && chatNumber === myLid);
+          if (!isSelfChat) continue;
+        }
+        // WHATSAPP_MODE === 'mixed': forward EVERY fromMe message (self-chat
+        // AND manual replies to clients) — the Python-side plugin decides what
+        // to do with each one (owner assistant vs. silence-on-manual-reply).
+        // No filtering here; `fromMe: true` is what lets it distinguish them.
       }
 
       // Handle !fromMe messages (from other people) based on mode.
@@ -301,6 +320,7 @@ async function startSocket() {
           } catch {}
           continue;
         }
+        // 'bot' and 'mixed' both gate non-owner messages through the allowlist.
         if (!matchesAllowedUser(senderId, ALLOWED_USERS, SESSION_DIR)) {
           try {
             console.log(JSON.stringify({
@@ -427,6 +447,7 @@ async function startSocket() {
         senderName: msg.pushName || senderNumber,
         chatName: isGroup ? (chatId.split('@')[0]) : (msg.pushName || senderNumber),
         isGroup,
+        fromMe: !!msg.key.fromMe,
         body,
         hasMedia,
         mediaType,
diff --git a/tools/memory_tool.py b/tools/memory_tool.py
index 281c806ea..7abe5ce13 100644
--- a/tools/memory_tool.py
+++ b/tools/memory_tool.py
@@ -121,14 +121,26 @@ class MemoryStore:
         Tool responses always reflect this live state.
     """
 
-    def __init__(self, memory_char_limit: int = 2200, user_char_limit: int = 1375):
+    def __init__(self, memory_char_limit: int = 2200, user_char_limit: int = 1375, contact_user_id: str = None):
         self.memory_entries: List[str] = []
         self.user_entries: List[str] = []
         self.memory_char_limit = memory_char_limit
         self.user_char_limit = user_char_limit
+        # When set, memory is scoped to this contact's subdirectory so that
+        # different WhatsApp contacts on the same profile never share memory.
+        self._contact_user_id: Optional[str] = contact_user_id
         # Frozen snapshot for system prompt -- set once at load_from_disk()
         self._system_prompt_snapshot: Dict[str, str] = {"memory": "", "user": ""}
 
+    def _get_memory_dir(self) -> "Path":
+        """Return the memory directory, scoped per-contact when running in gateway mode."""
+        import re as _re
+        base = get_hermes_home() / "memories"
+        if self._contact_user_id:
+            safe = _re.sub(r"[^a-zA-Z0-9_\-]", "_", self._contact_user_id)
+            return base / "contacts" / safe
+        return base
+
     def load_from_disk(self):
         """Load entries from MEMORY.md and USER.md, capture system prompt snapshot.
 
@@ -147,7 +159,7 @@ class MemoryStore:
         Scanning is deterministic from disk bytes, so the snapshot remains
         stable for the entire session (prefix-cache invariant holds).
         """
-        mem_dir = get_memory_dir()
+        mem_dir = self._get_memory_dir()
         mem_dir.mkdir(parents=True, exist_ok=True)
 
         self.memory_entries = self._read_file(mem_dir / "MEMORY.md")
@@ -242,9 +254,8 @@ class MemoryStore:
                     pass
             fd.close()
 
-    @staticmethod
-    def _path_for(target: str) -> Path:
-        mem_dir = get_memory_dir()
+    def _path_for(self, target: str) -> Path:
+        mem_dir = self._get_memory_dir()
         if target == "user":
             return mem_dir / "USER.md"
         return mem_dir / "MEMORY.md"
@@ -269,7 +280,7 @@ class MemoryStore:
 
     def save_to_disk(self, target: str):
         """Persist entries to the appropriate file. Called after every mutation."""
-        get_memory_dir().mkdir(parents=True, exist_ok=True)
+        self._get_memory_dir().mkdir(parents=True, exist_ok=True)
         self._write_file(self._path_for(target), self._entries_for(target))
 
     def _entries_for(self, target: str) -> List[str]:
diff --git a/tools/session_search_tool.py b/tools/session_search_tool.py
index 65b9d32f1..151d5308e 100644
--- a/tools/session_search_tool.py
+++ b/tools/session_search_tool.py
@@ -107,13 +107,16 @@ def _shape_message(m: Dict[str, Any], anchor_id: Optional[int] = None) -> Dict[s
     return {k: v for k, v in entry.items() if v is not None or k in ("content",)}
 
 
-def _list_recent_sessions(db, limit: int, current_session_id: str = None) -> str:
+def _list_recent_sessions(db, limit: int, current_session_id: str = None, contact_user_id: str = None) -> str:
     """Return metadata for the most recent sessions (no LLM calls, no FTS5)."""
     try:
         sessions = db.list_sessions_rich(
             limit=limit + 5,
             exclude_sources=list(_HIDDEN_SESSION_SOURCES),
             order_by_last_active=True,
+            # Contact isolation at SQL level: a gateway contact only browses
+            # its own sessions. None (CLI) browses everything as before.
+            user_id=contact_user_id,
         )  # fetch extra so we can skip current
 
         current_root = _resolve_to_parent(db, current_session_id) if current_session_id else None
@@ -156,6 +159,7 @@ def _scroll(
     around_message_id: int,
     window: int = 5,
     current_session_id: str = None,
+    contact_user_id: str = None,
 ) -> str:
     """Scroll shape: return a window of messages centered on an anchor.
 
@@ -200,6 +204,13 @@ def _scroll(
     if not session_meta:
         return tool_error(f"session_id not found: {session_id}", success=False)
 
+    # Contact isolation: a gateway contact may only scroll its own sessions.
+    # Without this, an enumerated/guessed session_id+message_id could surface
+    # another contact's transcript (discovery/browse are already scoped, so a
+    # legitimately-obtained id always passes). None (CLI) is unrestricted.
+    if contact_user_id and session_meta.get("user_id") != contact_user_id:
+        return tool_error(f"session_id not found: {session_id}", success=False)
+
     # Fetch the window
     try:
         view = db.get_messages_around(session_id, around_message_id, window=window)
@@ -281,6 +292,7 @@ def _discover(
     limit: int,
     sort: Optional[str],
     current_session_id: str = None,
+    contact_user_id: str = None,
 ) -> str:
     """Discovery shape: FTS5 + anchored window + bookends per hit. Single call."""
     role_list = role_filter if role_filter else ["user", "assistant"]
@@ -293,6 +305,11 @@ def _discover(
             limit=50,  # widen so dedup-by-lineage can find distinct sessions
             offset=0,
             sort=sort,
+            # Contact isolation pushed into SQL: when serving a gateway contact,
+            # FTS5 only matches that contact's own messages. This both enforces
+            # isolation and avoids the dedup-by-lineage limit being exhausted by
+            # other contacts' sessions before this contact's are reached.
+            user_id=contact_user_id,
         )
     except Exception as e:
         logging.error("FTS5 search failed: %s", e, exc_info=True)
@@ -344,6 +361,10 @@ def _discover(
         except Exception:
             session_meta = {}
 
+        # Contact isolation: skip sessions that don't belong to this contact.
+        if contact_user_id and session_meta.get("user_id") != contact_user_id:
+            continue
+
         entry = {
             "session_id": hit_sid,
             "when": _format_timestamp(
@@ -381,6 +402,7 @@ def session_search(
     limit: int = 3,
     db=None,
     current_session_id: str = None,
+    contact_user_id: str = None,
     # Scroll shape
     session_id: str = None,
     around_message_id: int = None,
@@ -414,6 +436,7 @@ def session_search(
             around_message_id=around_message_id,
             window=window,
             current_session_id=current_session_id,
+            contact_user_id=contact_user_id,
         )
 
     # Limit clamp [1, 10]
@@ -426,7 +449,7 @@ def session_search(
 
     # Browse shape: no query → recent sessions.
     if not query or not isinstance(query, str) or not query.strip():
-        return _list_recent_sessions(db, limit, current_session_id)
+        return _list_recent_sessions(db, limit, current_session_id, contact_user_id=contact_user_id)
 
     # Parse role_filter
     role_list: Optional[List[str]] = None
@@ -447,6 +470,7 @@ def session_search(
         limit=limit,
         sort=sort_norm,
         current_session_id=current_session_id,
+        contact_user_id=contact_user_id,
     )
 
 
